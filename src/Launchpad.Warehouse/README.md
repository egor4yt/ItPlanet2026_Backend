# Warehouse Service

The `Warehouse` service is a core component of the Launchpad ecosystem, responsible for managing metadata and reference data (such as job tags, skill definitions, and industry categories). It acts as the "source of truth" for shared domain attributes across the platform.